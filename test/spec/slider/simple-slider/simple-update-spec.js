describe('SimpleSlider update', () => {

  var chart;

  /*

  These tests will be implemented once we upgrade to D3 4.0+,
  which has an event dispatch method.

  beforeEach((done) => {
    chart = global.initSnapSlider(chart, done);

  });

  */

});
