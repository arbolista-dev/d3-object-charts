describe('SnapSlider update', () => {

  var chart;

  /*

  These tests will be implemented once we upgrade to D3 4.0+,
  which has an event dispatch method.

  beforeEach((done) => {
    chart = global.initSnapSlider(chart, done);

  });

  it('on tick click', (done) => {
    chart.onSnap = function(snap_value){
      expect(snap_value).toBe(70);
      expect(chart.current_value).toBe(70)
      done();
    }

    let tick = d3.selectAll('#snap-chart .tick')
          .filter(':nth-child(7)');


    // trigger click on tick here.
  });
  */

});
